# Runtime dependencies for the hermes-gadget modernized fork (Python 3.12+).
#
# NOTE: the LLM layer no longer uses the openai SDK. All model traffic goes
# through the OpenCode Go gateway via a small `requests`-based client in
# reverie/backend_server/persona/prompt_template/gpt_structure.py.
#
# The API key is read from ~/.opencode-go.key (or OPENCODE_GO_API_KEY env).
# Embeddings are served by the local LM Studio box (192.168.2.4:1234).

# Backend (reverie simulation server)
numpy>=1.25,<2
requests>=2.31

# Frontend (Django environment server)
Django==5.2.*
gunicorn>=23,<24
Pillow>=9.0
